def run_baseline(theme: str, meter: str):
    """
    Baseline pipeline:
    Pure LLM generation without formal validation.
    """
    print(f"Running baseline with theme='{theme}', meter='{meter}'")
